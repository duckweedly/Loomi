---
title: 当前状态
description: Loomi 当前已完成内容和建议下一步。
---

## 已有基础

当前仓库已经有 Web/Electron 前端壳、Go API/DB 基座、本地 identity/thread/message、M4 run/event/SSE、M3.5 前端 Agent runtime 骨架、M5 LLM Gateway 的 backend/provider/frontend 基础切片、M5.5 Settings Placeholder 的前端设置占位面、M6 Worker Job Pipeline 的 queued background execution MVP、M6.5 Real Testing Console and Background UX、M7 Tool Call Approval Execution + Tool Result Continuation 的最小闭环、M9 RunContext + Pipeline foundation 的最小薄片、M10 Persona/Skill foundation 的 persona 选择与版本快照薄片、M11 MCP stdio foundation 的本地 discovery/read-only candidate 薄片、M12/M12.5 MCP approval-gated execution 的单工具本地 stdio 执行薄片和真实本地 smoke closeout 证据、M13 Memory Foundation 的 PG-backed memory 第一实现薄片、M15 Chat real integrated smoke closeout 的 deterministic 后端证据链、M16 Work mode foundation 的最小计划/进度/artifact metadata UI 薄片、M17 Work artifact evidence closeout 的可重复 local seed + real API/browser evidence path、M18 Tool Runtime + Tool Catalog foundation 的统一 catalog/broker/API/Settings 只读薄片，M18.5 Local Provider Autodetect 的检测-only Settings/API 证据薄片，M19 Local Provider Opt-in Bridge 的 Local Codex 显式本会话启用薄片，M20 Local Codex Execution Bridge 的 fixture-backed Gateway 执行薄片，M21 Workspace Read Tools 的 read-only auto-approved workspace 工具薄片，M22 Bounded Agent Loop 的第一段运行时边界，M23 Workspace Mutation Tools 的 approval-gated 写入/编辑薄片，M24 bounded read-only command 工具薄片，M25 MCP Management + LSP Read-only Foundation 的 MCP 状态和只读 LSP 工具薄片，M26 Web Tools Foundation 的 auto-approved public web search/fetch 薄片，M27 Browser Automation Foundation 的 approval-gated public HTTP browser session 薄片，M28 PostgreSQL-backed non-executable text artifact 薄片，M29 PostgreSQL-backed coordination-only agent task 薄片，UI-01 Formal Interface Shell 的浅色桌面壳候选，以及 UI-02 Real Usage Readiness 的真实使用态收口候选。M21-M27 当前是候选状态；浏览器级 smoke 需在本轮收口验证中单独记录。

M13 Memory Foundation 的 Spec Kit 目录为 `specs/019-memory-foundation/`，M13.5 closeout 目录为 `specs/020-memory-real-pg-smoke-closeout/`。当前已实现第一薄片：PG v1 memory entries/search/write proposal、RunContext safe memory snapshot、approval-gated agent memory writes、用户查看/检索/删除记忆的最小 API/UI、tombstone deletion、redaction 和 source-run audit events。M13.5 已补真实 Postgres/httpapi smoke，覆盖 migrated `memory_entries`/`memory_write_proposals`、proposal/approve/list/search/RunContext/delete/idempotency/out-of-scope/redaction。它明确不包含向量库/embedding/RAG、OpenViking provider、marketplace/plugin、sandbox/browser/activity recorder、多 agent 长期记忆自动化、worker/job queue rewrite 或 MCP rewrite。

M14 Memory Management Audit UX 的 Spec Kit 目录为 `specs/021-memory-management-audit-ux/`。当前已推进到 full UX complete candidate：thread-scoped memory detail/delete 需要匹配 thread/source scope，thread list/search 缺少 `scope_id` 会返回参数错误，terminal run 后 memory proposal/approve/deny/delete audit 写入 durable `memory_audit_events`，redaction 覆盖 `/home`、Windows path、stdout/stderr、tool output、provider trace、key/env markers，list/search/audit API 与前端 client 统一到 `query/q`、`limit`、`scope_type`、`scope_id`、`source_thread_id`、`source_run_id`、`source_type`、`include_tombstoned`。Settings > Memory 已覆盖真实 approved/tombstoned list、search、grounded filters、safe detail panel、delete confirmation、loading/empty/error/deleted states、thread-scoped detail/delete context guard、真实 `/v1/memory/audit` history、backend unavailable/error/empty 不造假，以及 seeded browser smoke。M14.1 review fix 补齐 audit thread filter contract、memory list/audit/detail latest-request guard 和 delete confirmation/error polish。M14 仍不包含 distill 自动总结、OpenViking、vector/embedding/RAG、browser/activity recorder ingestion、MCP、worker queue、sandbox 或多 agent 重写。

Memory provider parity 后续候选已推进到 M70：Settings > Memory 具备 provider cards、modal 配置、Nowledge/OpenViking localhost detect、recent diagnostic/runtime errors、runtime error run/event display、snapshot/impression/content view、manual memory entry、proposal review/edit、Notebook 工具、provider-aware tool availability、prompt memory/notebook injection、external provider prompt recall 及 timeline event、OpenViking/Nowledge read/write adapters、Nowledge rich read adapters、OpenViking `fs/ls` connections、Nowledge prompt recall regression，以及 external `commit_after_run` provider commit。M72 另补模型 provider 503 diagnosis：配置完整显示为 `configured`，Settings/CLI doctor 通过安全 completion smoke 显示 `completion-ok` 或 `completion-failed-503`。当前仍不包含 OpenViking install/start/restart bridge、provider key discovery、remote scan、automatic provider enable、activity recorder ingestion、M81 provider router/fallback 或独立 vector store。

M15 Chat Real Integrated Smoke Closeout 的 Spec Kit 目录为 `specs/022-chat-real-integrated-smoke-closeout/`。当前候选证据是 gated backend smoke `TestM15ChatRealIntegratedSmoke`：通过 HTTP 创建 thread/message/model-gateway run，加载 approved memory snapshot，经 deterministic provider 请求 discovered + persona-allowed local stdio MCP tool，进入 approval required，通过 HTTP approve，worker 执行一次 MCP `tools/call`，记录 redacted result，provider continuation 写入 final assistant message，并从 replay API 验证 memory/MCP/approval/execution/continuation/completed 事件链。M15 是 closeout/evidence slice，不新增 sandbox、automation tools、activity recorder、OpenViking/vector/RAG/distill、marketplace/plugin、多 agent 或 worker queue rewrite。

M16 Work Mode Foundation 的 Spec Kit 目录为 `specs/023-work-mode-foundation/`。当前候选实现是在现有 `Thread.mode = work` 上增加 Work Plan View：从 messages、当前 run 和 run events safe metadata 投影 goal、steps、status、artifact references 和 recent progress。Work mode Composer 不再因为 `thread.mode = work` 被强制只读；无 active run、backend/provider 可用时可以发起真实 model_gateway run，active run、backend unavailable、provider unavailable、loading/error/no-thread 仍禁用。Artifact 第一版仅显示 title/type/source thread/run/summary/created/updated，secret-looking 内容 redacted，command/path/shell/browser/filesystem/executable metadata 不作为 action 暴露。M16 没有新增 backend API、task system、sandbox、filesystem/shell/browser tools、activity recorder、multi-agent 或 worker queue rewrite。

M17 Work Artifact Evidence Closeout 的 Spec Kit 目录为 `specs/024-work-artifact-evidence-closeout/`。当前候选实现新增 `LOOMI_SEED_SCENARIO=m17-work-artifact` local-dev/test seed：复用现有 productdata thread/message/run/event 服务创建 Work thread、message、current run 和 `work.plan.updated` metadata event，前端通过 real API replay 投影 goal、steps、status、artifact references 和 recent progress。Artifact cards 增加 redaction marker，但仍无 executable controls。M17 没有新增生产事件写 API、artifact execution/runtime、sandbox、shell/filesystem/browser automation、activity recorder、multi-agent、plugin marketplace、新 task system 或 worker queue rewrite。

M18 Tool Runtime + Tool Catalog Foundation 的 Spec Kit 目录为 `specs/025-tool-runtime-catalog-foundation/`。当前候选实现新增 safe tool catalog、read-only `GET /v1/tools/catalog`、Settings > Tools 只读 catalog、统一 `ToolBroker`/`ToolInvocation`/`ToolResult` envelope，并让 `runtime.get_current_time` 与本地 stdio MCP 执行都从 worker approved-tool resume 进入同一 broker。RunContext enabled tools 从 catalog、persona allowlist 和 MCP discovery schema hash 交集生成；未发现、未 allowlist、disabled/non-executable 或 schema mismatch 的工具会在执行前失败。M18 没有新增 workspace/shell/sandbox/browser/web/artifact tools、plugin marketplace、remote MCP/OAuth、Local Provider autodetect、多工具循环、多 agent 或 worker queue rewrite。

M18.5 Local Provider Autodetect 的 Spec Kit 目录为 `specs/026-local-provider-autodetect-foundation/`。当前候选实现新增 Claude Code/Codex 本地 provider 检测：支持 fixture HOME/CODEX_HOME/CLAUDE_CONFIG_DIR 和 env map，返回 safe capability fields，经 `GET /v1/local-provider-detections` 暴露，并在 Settings > Providers 显示 Local Claude Code / Local Codex detected/not detected、explicit opt-in 和 no secrets copy。M18.5 不执行 CLI/helper，不刷新 OAuth，不读 keychain，不调用外网，不保存 token，不自动启用或切换 provider。

M19 Local Provider Opt-in Bridge 的 Spec Kit 目录为 `specs/027-local-provider-opt-in-bridge/`。当前候选实现新增 `POST/DELETE /v1/local-provider-detections/{provider_id}/enable`，支持 Local Codex 检测为 available 后显式本会话启用；`GET /v1/model-providers` 只返回已显式启用的 local provider safe capability，不触发本机 auth detection。Local Codex 会标记 `local_provider`、`session_local`、`credential_reference=redacted` 和 `execution_state=unsupported`，Chat 仍阻止发送。M19 不能实际通过 Local Codex Chat；剩余 blocker 是 Local Codex execution bridge。

M20 Local Codex Execution Bridge 的 Spec Kit 目录为 `specs/028-local-codex-execution-bridge/`。当前候选实现选择 auth.json direct bridge，不调用 CLI、不刷新 OAuth、不读 keychain；Local Codex 仅在显式 detect + enable 后注册为现有 Gateway provider，`GET /v1/model-providers` 返回 `status=available`、`execution_state=supported`。Chat Composer 不再因 available/supported Local Codex 阻止发送，发送后走原有 model_gateway run、worker、Gateway、run events、SSE、RunTimeline/RunRail 路径。自动化证据是 temp `CODEX_HOME` + 本地 OpenAI-compatible fixture；真实本机 OAuth 是否可用取决于本机 token 对目标 compatible endpoint 是否有效，失败时记录 provider failure，不伪造回复。

M21 Workspace Read Tools 的 Spec Kit 目录为 `specs/029-workspace-read-tools/`。当前候选实现新增 `workspace.glob`、`workspace.grep`、`workspace.read` 三个 builtin workspace 工具：通过 M18 catalog、ToolBroker、RunContext 和 worker continuation 执行；选择 workspace root 后，这三个 bounded read-only 工具自动批准执行，写入/编辑/patch apply 仍 approval-gated；Work mode 会按最新用户意图收窄 enabled tools，日常问候不暴露 workspace/sandbox/agent/artifact/browser/web 工具，文件/目录任务才暴露 workspace read 工具；Chat mode 不扩大 workspace access。工具 root 在 run 创建时从本地用户持久化 workspace root 快照进 background job、RunContext 和 tool invocation；`/v1/workspace/root` 不再写进程级 `LOOMI_WORKSPACE_ROOT`，该 env 只作为本地测试 fallback 在 run 创建时读取；未设置目录时不再默认用户 Home，workspace 工具会在读取前失败，桌面端必须显式选择目录或测试环境显式设置 `LOOMI_WORKSPACE_ROOT`；拒绝路径穿越、绝对路径越界、symlink 越界和 `.env*`/`secrets/**`/private key/`.git` 等敏感路径；read/grep/glob 都有 limit/truncated/safe metadata，不调用 shell/rg。目录分类优先使用 `workspace.tree_summary` 或 `workspace.list_directory`，`workspace.glob` 仅用于文件名匹配或窄范围补充，`workspace.grep` 仅用于内容搜索。

M22 Bounded Agent Loop + Todo Foundation 的 Spec Kit 目录为 `specs/030-bounded-agent-loop-todo-foundation/`。当前已完成 US1 backend slice 并补上 code-agent loop hardening：continuation 不再对所有第二次 tool call 一律 `unsupported_tool_loop`，而是允许 Work-mode enabled workspace、bounded command、LSP、web、browser、artifact、coordination 和 todo 工具在前一个 tool terminal 后记录新的 approval-required tool call；同一 run 仍只允许一个 non-terminal tool call；超过六次 accepted tool call 会以 `tool_loop_limit_reached` 失败且不记录额外 tool call；重复 `tool_call_id` 会以 `duplicate_tool_call_id` 失败。HTTP smoke 已覆盖 `workspace.glob -> approve -> result -> workspace.read -> approve -> result -> final`，以及 `workspace.read -> workspace.edit -> sandbox.exec_command -> workspace.read -> final`。Provider request 会暴露 enabled builtin tool schemas，使用 `workspace_edit`、`sandbox_exec_command`、`todo_write` 这类 provider-safe 名称并映射回内部 dotted tool name。Runtime 会在 Work thread 工具请求和工具成功后追加 durable `work.todo.updated` snapshot；新增 approval-gated `todo.write` 让模型主动提交 bounded safe todo snapshot，并以 `updated_by=provider` 写入同一种 `work.todo.updated` 事件。WorkPlanView 可从真实 run events replay 工具任务状态；Chat mode 不追加该 Work todo snapshot。Todo metadata normalization、Work-mode replay projection 和 WorkPlanView todo render 已有测试覆盖。M22 的浏览器验收仍在后续任务中。

M23 Workspace Mutation Tools 的 Spec Kit 目录为 `specs/031-workspace-mutation-tools/`。当前候选实现新增 `workspace.write_file`、`workspace.edit`、`workspace.patch_preview` 和 `workspace.patch_apply`：通过 M18 catalog、ToolBroker、RunContext、approval-gated tool-call path 和 worker resume 执行；只允许 Work mode 启用，拒绝路径穿越、敏感路径、symlink 越界、invalid UTF-8 和 oversized mutation。`workspace.edit` 要求同一 run 先 `workspace.read` 目标文件，并在编辑前检查文件 size/mtime 未变化；成功结果返回 compact diff/snippet，同时支持 CRLF/LF 归一匹配、CRLF 写回保持、多行替换缩进保持，以及非 Markdown 尾随空格清理。`workspace.patch_preview` 复用同一替换逻辑但不写文件，只登记 run-local preview token；`workspace.patch_apply` 必须匹配 fresh preview 后才写入。Settings > Tools 和 RunRail 标出 high-risk/write-capable 或 preview-only。

M24 Sandbox Exec Command Tools 的 Spec Kit 目录为 `specs/032-sandbox-exec-command-tools/`。当前实现保留 builtin 名称 `sandbox.exec_command`，但真实能力收口为 bounded command/process slice，不是隔离 sandbox：仅 argv-form、Work-mode only、always approval required、通过 ToolBroker/SandboxToolExecutor 执行 allowlisted 读码或验证命令；当前允许 `pwd`、`ls`、`cat`、`head`、`tail`、`sed -n`、`wc`、`rg`、read-only git 子命令、`go test` 和 Bun/npm/pnpm 的 test/build 命令。`sandbox.start_process`、`sandbox.continue_process`、`sandbox.terminate_process` 复用同一 allowlist/workspace 边界，并用 run-scoped in-memory process store 返回/轮询/终止短生命周期进程；continue 支持 `cursor`/`next_cursor` 增量读取，并可对显式 stdin-enabled 的窄进程写入 bounded `stdin_text` + monotonic `input_seq`。`cwd` 绑定 workspace root，stdout/stderr 有 byte limit、timeout 和安全预览，non-zero exit 作为成功执行的 `exit_code` 记录。shell、python/node 任意脚本、brew、curl/wget/ssh、rm/chmod/kill、write-capable git、package install、hidden search、绝对/穿越/敏感路径参数、stopped/denied/terminal/out-of-scope call 都在 spawn 前或执行前拒绝。M24 仍不包含 true streaming stdout/stderr、persistent shell、container/syscall sandbox 或 PTY。

CLI Control Plane 当前新增 `cmd/loomi` 和 `internal/cli` 薄控制面：支持 `help`、`version`、`completion`、`doctor`、`status`、`config show/set/unset`、`chat`、`sessions list/resume`、`models list`、`personas list`、`tools list`、`mcp servers`、`lsp tools`、`artifacts list/read`、`memory list/search/show/audit`、`agent tasks/tools`、`browser tools/events`、`run`、`runs status/stop/attach/follow`、`events tail`、`approvals list/follow`、`approvals approve`、`approvals deny`，全部通过现有 HTTP API、SSE、run status、stop 和 approval endpoints，不直接访问 DB 或 ToolBroker。`loomi version` 暴露可注入 build metadata；`scripts/build-cli.sh` 可生成本地 `dist/loomi` 并注入 version/commit/date；`scripts/install-cli.sh` 可安装到 `~/.local/bin/loomi` 或指定 `TARGET`，默认不覆盖已有目标；`loomi completion bash|zsh|fish` 输出 shell completion。`loomi doctor` 会只读检查 resolved config、API readiness、provider 和 tool catalog。`loomi mcp servers` 只显示 safe MCP status，不泄露 command/args/env/secret/host path；`loomi lsp tools` 只过滤展示 LSP catalog，实际 LSP 执行仍在 Work-mode approval-gated run 内；`loomi artifacts list/read` 只读取 thread-scoped bounded excerpts，artifact 创建仍在 approval-gated run 内；`loomi memory list/search/show/audit` 只读取 safe memory summaries/details/audit，不提供写入、审批或删除入口；`loomi agent tasks` 只读取 thread-scoped coordination task 投影，`loomi agent tools` 只展示 `agent.*` catalog，不启动子模型 run；`loomi browser tools/events` 只展示 browser catalog 和 run event 中的 browser tool 生命周期，不直接控制浏览器 session。`loomi run` 默认 Work mode + `local_codex` provider，用于本地 dogfood run/event/tool approval 闭环；Local Codex 未显式启用时会按 API 错误失败。CLI 已支持本地 config defaults、env overrides、SSE 断线后用 `after_sequence` 续流并按 event 去重；text 输出会压缩展示工具参数/结果摘要，并对 workspace、sandbox、browser、artifact、web、LSP、todo 和 coordination 工具显示 `path=...`、`exit=...`、`stdout="..."`、`links=...`、`items=...` 等 per-tool 摘要；`run --compact` 可用同一套短 transcript 看 live run；`runs attach` 可回放已有事件并从最后 sequence 继续 streaming，`runs follow` 默认 future-only tail；`approvals approve|deny --follow` 可在审批后从当前 sequence 继续 tail，`run --interactive-approvals` 和 `loomi chat` 都可在终端中 approve/deny/skip 后自动续流，`tools list` 可按 group/enablement 聚焦展示，`events tail --tools-only --compact` 可只看工具链路；chat 内新增 `/tools [group]`、`/run [run-id]`、`/approvals [run-id]`、`/events [compact] [run-id]`、`/stop [run-id]` 方便长会话里直接检查上下文和停止 run。下一步主要补 release packaging；真正的子模型执行和直接浏览器控制仍要走后续 safety-gated 设计。

M25 MCP Management + LSP Read-only Foundation 的 Spec Kit 目录为 `specs/033-mcp-management-lsp-readonly/`。当前候选实现新增 read-only `GET /v1/mcp/servers` 和 Settings > MCP status，暴露安全的 local stdio MCP server/discovery 摘要，不返回 command/args/env/secrets/host paths。新增 builtin `lsp.diagnostics`、`lsp.symbols`、`lsp.references`、`lsp.definition`、`lsp.hover`，仅 Work mode 启用，always approval required，通过 ToolBroker/worker continuation 执行 bounded workspace-scoped read-only 查询。Chat mode、invalid args、traversal、absolute path、sensitive path、symlink escape、stopped/denied/terminal no-exec 均有测试覆盖。M25 不包含 MCP config write UI、remote MCP/OAuth、marketplace、真实 language server lifecycle、package-manager diagnostics、browser/web/artifact runtime 或多 agent 编排。

M26 Web Tools Foundation 的 Spec Kit 目录为 `specs/034-web-fetch-tool-foundation/`，搜索补丁目录为 `specs/041-web-search-providers/`。当前候选实现包含 builtin `web.fetch` 和 `web.search`：两者都可在 Chat/Work 中按 persona allowlist 启用，都是 read-only auto-approved public web 工具；`web.fetch` 执行一次 bounded public HTTP(S) 读取，`web.search` 通过 configured Tavily 或 Brave Search provider 查询公开网页结果。搜索 key 和本地 custom provider config 会通过 product data store 跨 API 重启保留，但 API/UI 只返回 key presence，不返回 key 原文；搜索结果只保留 bounded title/url/snippet。Web tools 不包含 JavaScript rendering、cookies、authenticated fetch、deep crawler、social media search、activity recorder 或多 agent 编排。

M27 Browser Automation Foundation 的 Spec Kit 目录为 `specs/035-browser-automation-foundation/`。当前候选实现新增 builtin `browser.open`、`browser.snapshot`、`browser.click_link`、`browser.screenshot`、`browser.type` 和 `browser.press`：仅 Work mode 启用，always approval required，通过 ToolBroker/worker continuation 执行 public HTTP(S)-only 的 run-scoped browser session。生产默认拒绝非 HTTP(S)、credentialed URL、localhost、loopback、private/link-local/multicast/unspecified host、blocked redirect 和 unsafe link target；结果只保留 safe URL/title/text excerpt/link/input/session metadata。`browser.screenshot` 是文本快照，不返回图片；`browser.type/press` 只更新 run-scoped session state，不执行 JS。M27 不包含 Chrome profile、cookies、JavaScript rendering、binary screenshots、downloads、authenticated browsing、crawler、artifact runtime、activity recorder 或多 agent 编排。

M28 Artifact Runtime Foundation 的 Spec Kit 目录为 `specs/036-artifact-runtime-foundation/`。当前候选实现新增 builtin `artifact.create_text`、`artifact.read` 和 `artifact.list`：仅 Work mode 启用，always approval required，通过 ToolBroker/worker continuation 创建、读取和列出 bounded UTF-8 text artifacts。真实 API 路径使用 PostgreSQL `artifacts`，in-memory 和 PG 均覆盖 create/read/list 与 cross-thread no-leak。结果只保留 safe title/type/size/excerpt/truncation/source metadata。M28 不包含 binary artifacts、downloads、rendered previews、iframe execution、filesystem export、browser integration、shell integration、artifact version graph、marketplace packaging 或 multi-agent 编排。

M29 Multi-agent Runtime Foundation 的 Spec Kit 目录为 `specs/037-multi-agent-runtime-foundation/`。当前候选实现新增 builtin `agent.spawn`、`agent.list` 和 `agent.complete`：仅 Work mode 启用，always approval required，通过 ToolBroker/worker continuation 创建、列出和完成 bounded coordination task records。真实 API 路径使用 PostgreSQL `agent_tasks`，in-memory 和 PG 均覆盖 spawn/list/complete 与 cross-thread no-leak。结果只保留 safe task id/role/goal/status/result summary/source metadata，并显式记录 `autonomous_execution=false`。M29 不包含 autonomous child model runs、cross-thread delegation、external worker pools、process spawning、filesystem access、network calls、shell execution、long-term multi-agent memory、marketplace packaging 或 background swarm orchestration。

原路线 M8 Worker + Job Queue 已由 M6 Worker Job Pipeline 覆盖并通过 013 closeout：`background_jobs` 表、API 同事务 queued run + job、worker claim、lease renew、retry/backoff、failed terminal、lost-lock ownership guard、202 queued acknowledgement、worker crash recovery、旧 worker 失锁后不能写 terminal 均已有证据或已由 closeout 补齐。M8 closeout passed；下一步不要重做 worker/job queue。

Spec Kit 已接入仓库，并写入 Loomi constitution。文档站已作为 `docs-site/` 独立子项目创建，用于长期记录技术细节。

已完成的可运行薄片：

- M1：桌面感 Web 壳和基础面板。
- M2：API、配置、PostgreSQL、migration、health/readiness 和 diagnostics 基座。
- M3：本地 identity、thread/message API、seed、mock/real API 切换。
- M3.5：Chat Canvas 状态机、mock runtime scripts、RunRail/Timeline/Agent badge 联动。
- M4：持久化 run/event、history-first SSE、local simulated run、stop/already-terminal 语义。
- M5：后端 LLM gateway、provider capability、provider-normalized run events、streaming assistant draft、redacted failure states 和 non-executed tool boundary。
- M5.5：Settings Placeholder、General session-local controls、read-only runtime/provider status，以及安全的 mock/preview future settings categories。
- M6：queued run acknowledgement、durable background job、local in-process worker、pipeline events、frontend queued/worker timeline replay、lease recovery、cancellation 和 worker diagnostics。
- M8 closeout：原 M8 Worker + Job Queue 已由 M6 覆盖；013 仅补齐 stale lease recovery 的 retry backoff scheduling，并记录审计证据。
- M6.5：Provider Test Console、real mode provider unavailable guidance、read-only Background tasks observer、M6 worker/job Timeline labels、Composer 状态收口和本地真实测试 runbook。
- M7 approval execution closure：approval-blocked tool-call projection、`runtime.get_current_time` allowlist/schema、tool lifecycle events、safe metadata redaction、worker diagnostics counters、frontend tool-event replay mapping、provider tool-call conversion、scoped tool-call read API、idempotent approve/deny API、enabled approval UI actions、approved current-time worker execution、result/error/denied SSE replay。
- M7 tool-result continuation closure：approved `runtime.get_current_time` 执行成功后自动续调 provider、provider-neutral continuation context、OpenAI-compatible tool-result serialization、`model_phase = continuation` replay、one final assistant message、denied/tool-failed no-continuation、continuation provider failure redacted terminal，以及 continuation 再请求工具时的 `unsupported_tool_loop` 安全失败。
- M9 RunContext/Pipeline foundation：worker 执行前从 durable run/thread/messages/job/provider route/tool summary 准备 RunContext，按 `prepare_context`、`resolve_tools`、`invoke_runtime`、`finalize` 记录线性 pipeline trace，Timeline/debug/Background tasks 可从 live SSE 与 history replay 看到安全摘要。
- M10 Persona/Skill foundation：内置 persona 配置同步到 DB，thread/run 可选择或继承 persona，run 创建时记录 persona snapshot/version，RunContext 应用 persona model route 和 allowed tool names，Timeline/debug 只显示安全 persona summary，前端提供最小 persona selector。
- M11 MCP stdio foundation：显式本地 stdio MCP 配置校验、bounded discovery/list-tools parser、namespaced read-only ToolSpec candidate、persona allowed-tools 非执行引用、RunContext MCP availability 安全摘要、Timeline/debug discovery 成功/失败/禁用标签；MCP 工具执行仍未开放。
- M12 MCP approval-gated execution：已发现且 persona allowed-tools 引用的本地 stdio MCP candidate 可以进入 M7 approval projection；approve 后由 worker 执行一个 bounded stdio `tools/call`，记录 redacted result/error，并进行一次 provider continuation；retry/recovery 不重复执行已 started/terminal 的 MCP call。
- M12.5 real MCP smoke closeout：新增真实本地 stdio MCP fixture 证据，覆盖 `Content-Length` discovery `tools/list`、`candidate_schema_hashes`、persona allowed tool、provider 请求 MCP tool、HTTP approve、worker 通过 `LOOMI_MCP_SERVERS_JSON` 使用 `StdioMCPToolExecutor` 执行一次 `tools/call`、redacted result、continuation 和 final assistant message。
- M13 Memory Foundation + M13.5 closeout：新增 `memory_entries`/`memory_write_proposals`、approved safe memory search、RunContext memory snapshot、`memory_snapshot_loaded`、approval-gated write proposal/approve/deny、source-run memory audit events、用户 list/search/delete API、Settings > Memory 最小 UI，并补真实 Postgres/httpapi smoke closeout 证据。
- M14 Memory Management Audit UX：Settings > Memory 已接真实 memory list/search/filter/detail/delete confirmation 和 `/v1/memory/audit` safe history；seeded browser smoke 覆盖 list、search/filter、detail、delete confirmation、post-delete refresh、`memory_deleted`/proposed/approved/denied/snapshot audit history，console 无 error。
- M14.1 Review Fix：memory audit filter shape 与 list/search 对齐，Settings memory list/audit/detail 加 latest-only guard，避免跨 thread history 混入和旧响应覆盖新状态。
- M15 Chat Real Integrated Smoke Closeout：gated backend smoke 覆盖 real API path、approved memory RunContext snapshot、MCP discovery candidate hash、persona allowed provider tool request、approval required、HTTP approve、worker MCP `tools/call`、redacted result、provider continuation、final assistant message 和 history replay。
- M16 Work Mode Foundation：Work mode thread 在主区域显示 Work Plan View，复用 existing thread/message/run/event 投影 goal、steps、status、artifact references 和 recent progress；Chat mode 不显示该 surface；artifact references 仅 metadata preview 且做 safe redaction。
- M17 Work Artifact Evidence Closeout：新增可重复 local-dev/test seed evidence path，使用现有 thread/message/run/event 数据链写入 Work metadata event；real API/browser smoke 可验证 Work Plan View、artifact redaction marker、无 executable artifact controls 和 Chat mode isolation。
- M18 Tool Runtime + Tool Catalog Foundation：新增 safe catalog、read-only Tools API、Settings > Tools catalog、统一 broker/executor envelope，并把 builtin current-time 与 local stdio MCP approved execution 接入同一执行入口。
- M18.5 Local Provider Autodetect：新增检测-only Claude Code/Codex local provider capability，read-only API 和 Settings > Providers 状态；检测结果不进入 model gateway provider list，不自动启用，不保存或显示 secrets。
- M19 Local Provider Opt-in Bridge：新增 Local Codex 显式 session-local enable/disable API、configured provider safe capability、Settings enable/disable UI 和 Chat unsupported 阻止；不执行真实 Local Codex 模型调用。
- M20 Local Codex Execution Bridge：新增 auth.json direct Local Codex provider，显式 enable 后返回 available/supported，并通过现有 worker/Gateway/run event/SSE 路径完成 fixture-backed Chat assistant reply；unsupported/unavailable local provider 仍阻止发送且不泄漏 token/path。
- M21 Workspace Read Tools：新增 read-only auto-approved `workspace.glob`、`workspace.grep`、`workspace.read`，绑定 workspace root/scope，拒绝 traversal/sensitive/symlink escape，结果 bounded/truncated，Settings > Tools 和 RunRail timeline 可见 workspace read-only 工具状态。
- M22 Code-agent Loop：bounded continuation 上限提高到 6，enabled workspace/mutation/sandbox/LSP/web/browser/artifact/agent/todo 工具可连续审批执行；HTTP smoke 覆盖 read -> edit -> exec -> read -> final，provider tool schemas 暴露 provider-safe function names；runtime 自动写入 durable work todo snapshots，`todo.write` 可写入 provider-maintained todo snapshots。
- Discovery Tools：新增 auto-approved `tool.load_tools` 和 `skill.load_skill`；前者按当前 run 已启用工具返回 safe catalog 描述，后者返回 installed skill manifest 摘要但不返回完整 `SKILL.md` 指令体；当前还不是动态 schema 注入。
- M23 Workspace Mutation Tools：新增 approval-gated `workspace.write_file`、`workspace.edit`、`workspace.patch_preview` 和 `workspace.patch_apply`，仅 Work mode 可启用，复用 ToolBroker/worker continuation，拒绝 traversal/sensitive/symlink escape/invalid UTF-8/oversize；edit 要求 read-before-edit、拒绝 stale edit，并返回 compact diff/snippet；patch preview/apply 支持先预览 diff、再用 fresh preview token 写入；RunRail 标出 high-risk/write-capable 或 preview-only。
- M24 Bounded Command Tools：保留 `sandbox.exec_command` 名称，并新增 `sandbox.start_process`、`sandbox.continue_process`、`sandbox.terminate_process`，仅 Work mode 可启用，argv-only、bounded timeout/output、workspace-root cwd、读码/验证 allowlist、run-scoped process handle、cursor polling、窄 stdin process，明确不是隔离 sandbox，RunRail 标出 high-risk/exec-capable/validation-capable，HTTP smoke 覆盖 approve -> execute -> continuation。
- M80 Durable Run Resume：worker 重试已 `tool_call_succeeded` 但尚未进入 continuation 的 tool job 时，会从 durable run events 恢复 provider continuation；若后缀里已有 continuation start、后续 tool request 或 terminal event，则不重复执行、不重复 final assistant。Pending `tool_call_requested` 不进入 continuation input。
- CLI Control Plane：新增 `cmd/loomi`，可从终端驱动 help/status/config/chat/sessions/models/personas/tools/run/events/approvals，并支持配置写入、工具参数/结果摘要、审批后 follow、交互式审批、分组工具视图和工具事件过滤/压缩视图，作为本地 code-agent 闭环的 dogfood surface。
- M25 MCP Management + LSP Read-only Foundation：新增 Settings/API MCP read-only status，以及 approval-gated `lsp.diagnostics`、`lsp.symbols`、`lsp.references`、`lsp.definition`、`lsp.hover`；LSP 工具仅 Work mode 启用，workspace-scoped、bounded、read-only，RunRail 标出 low-risk/read-only/workspace-scoped。
- M26 Web Tools Foundation：新增 auto-approved `web.search` / `web.fetch`，Chat 和 Work 均可按 persona allowlist 启用，public web only、bounded timeout/bytes/redirect/results、拒绝 private/local/credentialed targets，RunRail 标出 medium-risk/public HTTP-only。
- M27 Browser Automation Foundation：新增 approval-gated `browser.open`、`browser.snapshot`、`browser.click_link`、`browser.screenshot`、`browser.type` 和 `browser.press`，仅 Work mode 可启用，public HTTP(S)-only、run-scoped stateful session、bounded title/text/link/input summaries，RunRail 标出 medium-risk/public HTTP-only。
- M28 Artifact Runtime Foundation：新增 approval-gated `artifact.create_text`、`artifact.read` 和 `artifact.list`，仅 Work mode 可启用，PG-backed bounded text-only、non-executable、safe summaries，RunRail 标出 medium-risk/non-executable。
- M29 Multi-agent Runtime Foundation：新增 approval-gated `agent.spawn`、`agent.list` 和 `agent.complete`，仅 Work mode 可启用，PG-backed coordination-only、no autonomous execution、safe summaries，RunRail 标出 medium-risk/no autonomous execution。
- UI-01 Formal Interface Shell：新增浅色 desktop shell、窄 sidebar、白色 canvas、固定 composer 和 secondary panel containment，保持 Chat/Work、Settings、Tools、RunRail、provider warning 和 Stop 可用。
- UI-02 Real Usage Readiness：移除 Chat 旧 runtime/context 顶栏和 composer 假入口，保留 provider blocker 警告；WorkPlanView 不再从普通消息冒充计划；RunRail/ToolCallCard 使用人话工具标签；approval blocked 显示等待确认；Sidebar 移除重复搜索框和底部新建/搜索操作簇，折叠态顶栏 compose 可创建当前模式 thread，保留 thread 重命名和删除；Composer 保留输入、run actions、发送和模式 placeholder；Chat 消息支持基础 Markdown。该轮不新增 backend/runtime/provider/tool/DB/M38 能力。

## 建议下一步

UI-02 完成后，先做全量 validation 和浏览器 smoke closeout，确认真实使用态没有隐藏 provider、approval、thread actions、Markdown 或工具事件可读性问题。不要在 UI-02 里推进 M38/activity recorder，也不要新增工具能力。

M21 已作为 Workspace Read Tools validated candidate，保持其 read-only workspace scope，不在 M21 临时塞入 write/edit、shell、browser、web、artifact 或 multi-agent runtime。

从最小 code-agent 闭环看，Loomi 已补上 `read -> edit -> exec -> read -> final` 的 approval-gated 后端闭环、provider tool schema 暴露、runtime todo snapshot、provider-driven `todo.write`，以及 `patch_preview -> patch_apply` 的 review-first 文件修改路径。CLI 已开始作为薄控制面复用 HTTP/SSE/approval 和后端 worker，不直接碰 DB、ToolBroker 或文件系统，并已补 config 写入、approval follow、SSE 断线续流、工具参数/结果终端布局、doctor/version、completion、本地 build/install、长 run attach/follow、MCP safe status、LSP catalog visibility、artifact read-only projection 和 memory read-only visibility；后续差距集中在 browser/sub-agent 等工具面的横向覆盖和 release packaging。

推荐后续顺序：

1. **完成 UI-02 closeout**：跑全量 validation、docs build、browser smoke，并确认 Mock/Real、Work folder、WorkPlanView、RunRail human labels、approval blocked、Sidebar 精简入口和 Composer 文案符合真实使用态要求。
2. **保持 `specs/038-activity-recorder-foundation/` 为 draft/future**：不要纳入当前完成项或当前验证范围。
3. **后续 multi-agent 扩展**：只有在 M29 coordination task record 可靠后，才考虑 autonomous child runs、cross-thread handoff 或外部 worker pool。

Web Search/Fetch、Browser Automation、Artifact runtime、Channels、Heartbeat、Desktop Runtime、Plugin、Activity Recorder、多 agent 编排和复杂 marketplace 都应排在 bounded loop、workspace mutation、sandbox exec、安全边界和审计之后。

## 开发要求

后续非平凡开发必须同步更新 `docs-site/`。M5.5 已更新 Settings 架构页、runbook、开发日志和验证命令；M6 已新增 worker/job pipeline API、架构页、runbook、开发日志和 Spec Kit 状态；013 已记录 M8 closeout passed 和 retry/backoff 补丁；M6.5 已新增 Provider Test Console/Background tasks 架构页、本地 provider testing runbook、开发日志和验证命令；M7 已新增并更新 tool-call approval architecture/API/runbook，并新增 tool-result continuation architecture/devlog；M9 foundation 已更新 worker-job pipeline 架构/API、本地 M9 runbook、devlog 和 Spec Kit 状态；M10 foundation 已新增 persona architecture/API/runbook/devlog，并更新当前状态与 Spec Kit 状态；M11 foundation 已新增 MCP stdio architecture/API/runbook/devlog，并更新当前状态 与 Spec Kit 状态；M12/M12.5 已新增并补齐 MCP approval execution architecture/API/runbook/devlog、真实本地 smoke closeout 证据，并更新当前状态与 Spec Kit 状态；M13/M13.5 Memory Foundation 已新增 memory architecture/API/runbook/devlog、真实 PG/httpapi smoke closeout，并更新当前状态与 Spec Kit 状态；M14/M14.1 已补 memory management/audit UX contract、完整 Settings > Memory UX、seeded browser smoke 和 review fix 证据；M15 已补 Chat integrated smoke runbook/devlog 和 Spec Kit 状态；M16 已补 Work mode foundation architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M17 已补 Work artifact evidence closeout runbook/devlog、architecture/API、roadmap 和 Spec Kit 状态；M18 已补 tool runtime catalog architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M18.5 已补 local provider autodetect architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M19 已补 local provider opt-in architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M20 已补 local codex execution architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M21 已补 workspace read tools architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M23 已补 workspace mutation tools architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M24 已补 sandbox exec command architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M25 已补 MCP/LSP read-only architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M26 已补 web fetch architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M27 已补 browser automation architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M28 已补 artifact runtime architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M29 已补 multi-agent runtime architecture/API/runbook/devlog、roadmap 和 Spec Kit 状态；M80 已补 durable run resume architecture/API/devlog/spec-kit 状态；UI-01/UI-02 已补 formal interface shell architecture/devlog、roadmap 和 Spec Kit 状态。
